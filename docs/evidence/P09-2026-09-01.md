# P09 retained evidence — Generate a Diagnostic Residual

**Batch:** P09
**Date:** 2026-09-01 UTC
**Guiding question:** What inputs, observable effects, and failure modes matter when you generate a Diagnostic Residual?
**Disposition / evidence status:** implemented; P09 is `implemented` with evidence level `simulated`
after the isolated candidate passed focused, contract, quick, full, and CLI lifecycle gates.

## Claim boundary

This record supports static repository structure, independent Python reference arithmetic, synthetic
deterministic signal contracts, CLI behavior, and retained command output only. The manifest's
`simulated` evidence level classifies the module's synthetic model; it
does not mean MATLAB executed here. No MATLAB-runtime, rendered-UI, MATLAB numerical-fidelity,
protocol, bench, HIL, RT1/RT2, field, playtest, staging, signing, deployment, production, or physical
validation is claimed.

## Acceptance map

| Acceptance item | Implementation evidence | Current disposition |
| --- | --- | --- |
| Preserve the exact guiding question | Manifest identity plus README, lesson, walkthrough, `lesson.m`, and `experiment.m` | Static source present |
| Read → baseline → one lever → changed view → mechanism-first explanation | `lesson.m`, sectioned `experiment.m`, tutor lesson, and walkthrough | Static source present; human session unperformed |
| Deterministic baseline, labels, metrics, two sweeps, one broken case | Fixed 301-sample signals; three baseline views; effectiveness-loss and predictor-gain sweeps; omitted-command case | Independent references and static tests pass |
| Separate deterministic model and interaction | `model.m` contains calculations and metrics only; `interactive.m` owns three bounded controls, reset, and one `uiaxes` | Static source tests pass; MATLAB/UI unperformed |
| Connect to prerequisites without syntax-first teaching | P08 `A only` degraded-success priority is separated from P09 conditional loss magnitude before equations or controls | Static narrative review complete |
| Independent invariants, limits, interpretation, teach-back | `run_checks.m` and `checks.md` cover identities, RMS/means, pointwise sweep decomposition, cancellation, full/zero limits, malformed inputs, recovery, and teach-back | Static inspection and Python references pass; MATLAB checks unperformed |
| Status changes only after focused and repository-wide verification | Isolated implemented/simulated copy passed 14 focused and 113 repository tests plus all three profiles before the real P09-only transition; the repaired final candidate passes 15 focused and 114 repository tests | Gate passed; ordering and final regression state retained |
| Honest evidence taxonomy | This claim boundary, runtime inventory, behavior matrix, residual risks, and unperformed-validation list | Present |

## Independent deterministic references

The retained Python tests independently calculate rather than execute MATLAB source:

- fixed time grid: `0:0.1:30`, 301 samples;
- command: `0.5` before 10 seconds and `0.8` afterward;
- nominal gain: `10 L/min per normalized command`;
- baseline mean flows: `5`, `8`, and post-loss `6.4 L/min`;
- residual convention: `r=y-y_hat(u)`, negative when measured flow is below prediction;
- baseline healthy high-command mean `0 L/min`, post-fault mean and shift `-1.6 L/min`;
- healthy RMS `0.1/sqrt(2)=0.07071067811865475 L/min` and post-fault RMS
  `sqrt(1.6^2+0.1^2/2)=1.6015617378046967 L/min`;
- effectiveness-loss sweep `[0,0.05,0.10,0.20,0.30]` with post-fault means
  `[0,-0.4,-0.8,-1.6,-2.4] L/min`;
- predictor-gain sweep `[8,9,10,11,12] L/min/command` with healthy means
  `[1.6,0.8,0,-0.8,-1.6] L/min`, post-fault means `[0,-0.8,-1.6,-2.4,-3.2] L/min`, and constant
  fault-induced shift `-1.6 L/min`;
- healthy omitted-command broken case: correct command-step mean change `0 L/min`, broken change
  `+3 L/min`.

These references check intended arithmetic independently but are not MATLAB numerical-fidelity
evidence.

## Figure, control, metric, and unit inventory

### Experiment figures

1. Measured and command-conditioned predicted Pump-A flow against time, in `L/min` and seconds.
2. Signed diagnostic residual `r=y-y_hat` against time, in `L/min` and seconds.
3. Model-mismatch, physical-loss, deterministic-ripple, and summed residual decomposition, in
   `L/min` and seconds.
4. Post-fault mean residual against conditional effectiveness loss, in `L/min` and percent.
5. Healthy and post-fault mean residual against predictor gain, in `L/min` and
   `L/min per normalized command`.
6. Correct and broken healthy residual across a normal command change, in `L/min` and seconds.

### Interactive controls

| Control | Unit | Static bounds/default |
| --- | --- | --- |
| Conditional effectiveness loss after 20 s | percent | `[0,100]`; `20` |
| Predictor gain `K_hat` | L/min per normalized command | `[0,20]`; `10` |
| Deterministic ripple amplitude | L/min | `[0,1]`; `0.10` |
| Visible view | categorical flow/residual/decomposition | diagnostic residual default; one axes |
| Reset baseline | action | restores all three numeric inputs and residual view |

### Model outputs and metrics

- Fixed 301x1 time, command, activation, physical-flow, measured-flow, predicted-flow, residual,
  component, and broken-residual vectors.
- Scalar sample period/count, command and fault times, nominal/predictor gain, loss, ripple
  frequency/amplitude, and unit/sign/assumption contracts.
- Equal three-cycle low-command healthy, high-command healthy, and post-fault reference masks.
- Reference means, command-step change, fault-induced shift, healthy/post-fault RMS, expected
  signatures, and maximum decomposition closure error.
- Resource contract: three bounded scalars, fixed 301 samples, no input-sized loop, file, network,
  process, timer, asynchronous work, blocking prompt, or persistent/global state.

## Runtime inventory

| Item | Required by implementation | Observed in this environment |
| --- | --- | --- |
| Python 3 | Static verifier, independent arithmetic, isolated CLI tests | Python 3.11.2; executed |
| Base MATLAB | Model, figures, controls, and executable checks | Not installed; MATLAB execution unperformed |
| MATLAB toolboxes | None | Not queried because MATLAB is absent |
| Octave fallback | Not an approved substitute for the `uifigure` lesson | Not installed |
| Data/network/service | None | No module dependency; CI inspection was read-only |
| Hardware/bench/HIL/field | None for implementation | No validation performed |

## Focused behavior matrix

| Category | Retained check | Current result |
| --- | --- | --- |
| Positive | Exact equations, fixed signals, sign/units, means/RMS, pointwise decomposition across both sweeps, and learner flow | Independent Python/static source pass |
| Negative | Omitted-command false `+3 L/min` signature and gain-8 fault cancellation | Independent arithmetic/static source pass |
| Malformed input | MATLAB source rejects nonscalar, below/above-bound, NaN, Inf, and complex values for all three inputs | Present; MATLAB execution unperformed |
| Timeout | Shared CLI subprocess tests have ten-second limits; fixed model has no wait or variable work | Baseline CLI suite passed; model timing unmeasured |
| Cancellation | No request, job, timer, blocking prompt, partial write, or asynchronous lifecycle exists | Not applicable by static review |
| Rollback | Reversible file/status rollback below; no migration or database exists | Documented; not executed |
| Recovery | A valid baseline is recomputed exactly after rejected calls; CLI fixtures isolate learner state | Source present; MATLAB recovery unperformed |
| Isolation | Loss sweep locks predictor/pre-fault signals and fault attribution; gain sweep locks physical signals and mismatch attribution | Independent/static source pass |
| Compatibility | Transparent base-MATLAB arithmetic and established `uifigure` constructs; no toolbox path | Static only; release matrix unperformed |
| Resource bound | Three bounded scalars, 301 samples, fixed vector sizes, no external or asynchronous operation | Static source pass |

## Repository, ownership, concurrency, runtime, and CI inspection

- Initial branch `agent/p09` was at exact `origin/main` commit
  `f4c446984dc914586837052247300dd20f32cb44`, containing implemented P08.
- Initial target status contained only the inherited activation change
  `contracts/active-batch.yaml`; it remains a forbidden user/control-plane path.
- The separate local-main worktree remains at `e604208c90c5424e6bd9dbcc22837b0fb2228c32`.
- No target `CODEOWNERS`, dedicated ADR, safety record, compatibility record, Git lock, open target
  PR, or competing writer was found in the point-in-time inspection. Canonical `repos.yaml` assigns
  module/curriculum/validation/evidence ownership to this target repository.
- `portfolio status reliability-fdir-learning` reported P09 next, no stop gate, target open PRs `0`,
  and a control-pointer drift that is outside this batch and remains preserved.
- Latest starting-HEAD target Quality run `33463652073`, job `99718930827`, succeeded on exact
  `f4c4469`; P09 CI cannot run without a forbidden commit/push.
- `matlab` and `octave` were absent from `PATH`; this is not a stop because the contract supplies a
  transparent base-MATLAB source and static-reference path.

## Principal integration completion audit

The final integration pass began at `f4c446984dc914586837052247300dd20f32cb44` with the complete
P09 candidate already present in the worktree. The whole-worktree diff also contained two
control-plane-owned files that P09 explicitly forbids: `contracts/active-batch.yaml` (the staged
P08-to-P09 activation plus an unstaged control-revision refresh) and
`contracts/repo-profile.yaml` (an unstaged source-revision refresh). Both contract files had a
`2026-09-01 03:31:01 UTC` modification time before this integration pass changed any batch-owned
file. They were inspected, not edited or reverted, and are excluded from the P09 implementation
change set and validation claim.

Current preserved hashes for that external control-plane state are
`bf544b483e977c60ec464409fc196f84eb47ff87f2bd4401e814bf8a1235dffa` for
`contracts/active-batch.yaml` and
`3fc0f06900971bc36e6c68d1595df90d11440c722e799ebfe08b4bf68da05c87` for
`contracts/repo-profile.yaml`. The protected AGENTS, phase manifest, Makefile, and Quality workflow
still match the hashes recorded below. No additional target worktree uses `agent/p09`, no Git lock
or open target pull request was visible, and `origin/main` still equals the starting HEAD.

Live CI inspection found target Quality run `33463652073` successful on exact starting commit
`f4c4469`. Control-plane validation run `33466059669` was successful on exact recorded control
revision `46c6ef63c946cf43111f8fb04e6bc49dd125c5d2`. Neither run contains the uncommitted P09 changes;
commit-level P09 CI therefore remains unperformed. `portfolio status reliability-fdir-learning`
reported P09 next, no stop gate, target open pull requests `0`, and control-pointer drift outside
this batch. MATLAB and Octave remained unavailable.

Two bounded read-only completion audits independently reviewed the final candidate. The
math/MATLAB-source audit found no residual-equation, sign, unit, window, RMS, sweep, cancellation,
limit, UI-bound, or resource-contract defect. The system/contract audit found no batch-owned scope,
acceptance, permanence, test-category, newline, rollback, or evidence-boundary defect after the
external contract refreshes were explicitly excluded. Both audits retain the MATLAB-runtime, UI,
release-compatibility, profiler, human-teach-back, rollback-rehearsal, and commit-level-CI gaps.

This pass found and repaired one batch-owned hygiene defect: three trailing spaces in this evidence
header made the initial `git diff --check HEAD` exit `2`. Removing those spaces changed no claim or
implementation behavior; the post-repair command exits `0`.

| Command, run from the repository root | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p09_module.py` | 0 | No output |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 15 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 114 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 114 tests, `OK`; started `2026-09-01T03:38:36+00:00`; completed `2026-09-01T03:38:38+00:00`; log `docs/evidence/local/verify-20260901-033836.log` |
| Post-repair `git diff --check HEAD` | 0 | No output |
| Raw-byte EOF scan over every changed file | 0 | Every changed text file ends with exactly one newline |

## Independent audit findings and repairs

- A read-only governance audit confirmed the active/canonical P09 contract alignment, authorized
  scope, inherited forbidden-path change, clean ownership/concurrency state, successful starting
  CI, and absence of a stop gate. It made no edits.
- A read-only pattern audit checked the P08-to-P09 teaching bridge, reference arithmetic, phase
  boundary, artifact split, coverage applicability, and lifecycle gate before implementation. Its
  recommended deterministic residual structure and permanent-test constraints are implemented.
- A read-only math/source audit verified equations, signs, units, RMS references, both sweeps,
  cancellation, and the broken predictor. It found that the flow view's zero-based lower axis
  clipped valid negative measurements at maximum loss/ripple and that `eps(5)` had been described
  too broadly. The flow bounds now derive from measured/predicted data, the check names
  floating-point spacing near `5 L/min` accurately, and a focused regression locks the axis rule.
- A read-only contract audit found that an entirely one-sided residual could clip the drawn zero
  reference. Residual bounds now explicitly include zero, and a focused regression locks that
  behavior. Its remaining scope, lifecycle, persistence, content, test-category, evidence, and
  newline review was clean.
- Both implementation reviews were repeated against the repairs. No remaining material source,
  math, phase-boundary, scope, or test finding was reported. The reviewers did not edit files;
  MATLAB rendering and callback behavior remain unperformed.

## Independent system-risk review and repair

The independent system-risk review found one moderate missing-test risk, not a current model defect.
The baseline checked decomposition closure, and both sweeps checked aggregate means plus selected
non-target signals, but neither sweep locked the full component histories at nonbaseline settings.
A stale or cross-coupled `modelMismatchResidualLpm` or `faultResidualLpm` output could therefore
misattribute the discrepancy in the learner-visible decomposition while preserving every existing
sweep mean, baseline closure check, and isolation assertion.

The bounded repair adds independent pointwise assertions at all five values in each sweep. The loss
sweep now requires zero mismatch, `-10*L*f(t)*u(t)` fault attribution, fault-plus-ripple residual,
and decomposition closure across all 301 samples. The gain sweep requires `K_hat*u(t)` prediction,
`(10-K_hat)*u(t)` mismatch attribution, mismatch-plus-fixed-fault-plus-ripple residual, and closure.
The new independent Python behavior regression
`test_sweeps_preserve_pointwise_residual_component_attribution` evaluates those full histories,
their expected directions and isolation, confirms that the interactive decomposition consumes the
four checked outputs, and locks the matching MATLAB assertions in source. `checks.md` now names the
coverage. No model equation, output, callback, learner control, or shared public interface changed.

| Command, run from the repository root | Exit | Relevant result |
| --- | ---: | --- |
| Pre-repair `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 14 tests, `OK`; confirmed the pointwise nonbaseline decomposition gap was not covered |
| Pre-repair `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 113 tests, `OK` |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p09_module.py` | 0 | No output |
| Post-repair `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 15 tests, `OK`; includes the new pointwise component-attribution regression |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 114 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 114 tests, `OK`; started `2026-09-01T03:26:01+00:00`; completed `2026-09-01T03:26:03+00:00`; log `docs/evidence/local/verify-20260901-032601.log` |
| Final evidence-present `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 114 tests, `OK`; started `2026-09-01T03:26:51+00:00`; completed `2026-09-01T03:26:53+00:00`; log `docs/evidence/local/verify-20260901-032651.log` |
| Independent-review `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 114 tests, `OK`; started `2026-09-01T03:46:56+00:00`; completed `2026-09-01T03:46:58+00:00`; log `docs/evidence/local/verify-20260901-034656.log` |

The Python regression is independent reference arithmetic, not execution of `model.m`. The matching
MATLAB assertions remain retained but unexecuted because neither MATLAB nor an approved substitute
is installed. This review produced no MATLAB-runtime, rendered-UI, numerical-fidelity, bench, HIL,
RT, field, release, deployment, or production evidence.

A fresh whole-diff review traced the repaired decomposition fields through `model.m`, both sweeps,
the interactive decomposition, executable MATLAB checks, focused Python checks, manifest/CLI
publication, rollback language, and evidence claims. It found no additional material batch-owned
correctness, lifetime, resource, migration, compatibility, security, licensing, recovery, or
missing-test defect. The pre-existing control-plane changes under `contracts/` remain outside P09's
allowlist and were inspected but neither edited nor reverted.

## Exact validation commands and results

### Pre-change baseline

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 99 tests, `OK` |

### Initial source iteration while lifecycle remains scaffolded

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p09_module.py` | 0 | No output |
| Initial `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 1 | 14 tests ran; expected lifecycle/evidence failures plus three source-test findings, all repaired |
| Final pre-lifecycle `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 1 | 13 tests passed; only the intentional real-manifest lifecycle assertion remained |
| `git diff --check` | 0 | No whitespace errors |
| P09 placeholder and opaque-dependency scans | 1 per scan, expected no-match | No placeholder, randomness, opaque diagnostics call, external I/O/process, async work, or blocking prompt |

### Isolated implemented-candidate lifecycle gate

The isolated copy at `/tmp/reliability-fdir-p09-candidate.VNSR6k/repo` contained these sources and
changed only P09 lifecycle values to `implemented`/`simulated`. Every row below passed before the
real manifest changed.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 14 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 113 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Started `2026-09-01T03:08:04+00:00`; 113 tests, `OK`; completed `2026-09-01T03:08:06+00:00` |
| `./bin/learn start P09`, `./bin/learn check P09`, `./bin/learn status` in isolated state | 0 | Exact title/question; `Status: implemented`; launch/check commands; 24 total and 9 implemented |

The candidate full log is retained at
`/tmp/reliability-fdir-p09-candidate.VNSR6k/repo/docs/evidence/local/verify-20260901-030804.log`.

### Real-worktree verification after manifest transition

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 14 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 113 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Started `2026-09-01T03:08:30+00:00`; 113 tests, `OK`; completed `2026-09-01T03:08:32+00:00` |
| Isolated-state `./bin/learn start P09`, `./bin/learn check P09`, `./bin/learn status` using the real manifest | 0 | Exact title/question; `Status: implemented`; check command; 24 total and 9 implemented |

The real full-verification log is retained under the ignored local-evidence path
`docs/evidence/local/verify-20260901-030830.log`.

### Final post-evidence verification

The evidence content and both final-audit repairs were present for every command below.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p09_module` | 0 | 14 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 113 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Started `2026-09-01T03:16:48+00:00`; 113 tests, `OK`; completed `2026-09-01T03:16:49+00:00` |

The final post-evidence full log is retained under the ignored local-evidence path
`docs/evidence/local/verify-20260901-031648.log`.

## Changed and preserved invariants

- P09 owns one complete nine-file module artifact set, one permanent-fact focused test file, three
  shared learner entry-point/index updates, one completed P09-only manifest lifecycle transition, and
  this retained evidence.
- Exactly 24 modules retain canonical IDs, order, titles, questions, phases, folders, and
  earlier-only prerequisites; implemented modules must remain a contiguous canonical prefix.
- P09 retains prerequisite P08 and the exact approved guiding question.
- P01-P08 and P10-P24 implementation areas are not edited.
- Every active-batch forbidden path is preserved, including inherited dirty active-batch content,
  CI, agent skills, phase data, architecture, implementation contract, tutor protocol, and Makefile.
- Initial preserved SHA-256 values include AGENTS
  `6e769a084d6583a4e35820803fa46f51749889c7bd30731443ddd62276390e84`, active batch
  `8f1937060ef40ba15c209155d27b2c9aedf89f1a648a683b8f7ad127aba1a689`, phases
  `a8cc51f8d28ec40bbf633605b5ffec778635873a43aacda410201cda2983bee3`, Makefile
  `1af33be25ab25c149afe328a319a7fbd9d67fae8b94a42943b6b797ead2a49e8`, and Quality workflow
  `0a2617414f5f8249a25ed23d1b08f1cb78ae68f2d02650cc6bc07590b11c8d71`.
- No toolbox, random input, data/network/service dependency, learner-progress file, migration,
  database, deployment operation, or shared mutable module state is introduced.
- No commit, push, pull request, merge, release, deployment, or setting change is performed.

## Rollback

Rollback is file-based and has not been executed:

1. Restore the original six P09 scaffold files and remove P09 `model.m`, `interactive.m`, and
   `run_checks.m`.
2. Restore only P09 manifest status to `scaffolded` and evidence level to `none`.
3. Restore P09 frontier references in `README.md`, `START_HERE.md`, and `modules/README.md`.
4. Remove `tests/test_p09_module.py` and this retained evidence record.
5. Run all three active-batch validation commands and confirm P01-P08 remain the implemented prefix.

No backup restore, migration rollback, database recovery, service recovery, generated binary, or
external-system compensation is required.

## Residual risks

- MATLAB syntax, numerical behavior, graphics, callbacks, and `run_checks.m` remain unverified
  without a licensed MATLAB runtime.
- Static source checks cannot prove compatibility with every MATLAB release; no minimum release or
  release matrix is declared.
- The zero-delay gain model omits pump dynamics, time alignment, calibration drift, saturation,
  temperature/load coupling, correlated disturbances, and sensor faults.
- Predictor mismatch can create a healthy residual or cancel a fault at one operating point; the
  lesson demonstrates but does not solve robust residual design.
- The fixed deterministic ripple is not field noise, a probability distribution, a false-alarm
  characterization, or physical evidence.
- Residual sensitivity does not establish fault specificity, threshold adequacy, diagnosis
  probability, recovery safety, or mission-level failure.
- Static Python arithmetic is independent of MATLAB source but is not MATLAB numerical-fidelity
  evidence or educational-effectiveness evidence.
- Historical protected governance prose still names older frontiers/repository provenance; it is
  outside the P09 allowlist and preserved rather than silently rewritten.
- P09 target Quality and Portfolio Control CI cannot run without a commit or push, which the user
  explicitly forbids.

## Unperformed validation

- MATLAB execution of `model.m`, `experiment.m`, `interactive.m`, `lesson.m`, or `run_checks.m`.
- MATLAB numerical fidelity, profiler/timing/resource measurement, release compatibility matrix,
  toolbox/license resolution, or Octave execution.
- Rendered traces, axes, labels, legends, UI layout, callbacks, reset behavior, accessibility, or UI
  behavior and interaction cancellation.
- Human tutor walkthrough, interpretation answers, teach-back, learner completion, playtest, or
  learning-efficacy evaluation.
- Protocol validation, backup/restore smoke, migration/rollback rehearsal, and service smoke; no
  protocol, database, migration, backup, or service is introduced, so these profile gates are not
  applicable beyond documented review.
- Physical Pump-A/cooling-loop hardware, bench, HIL, RT1/RT2, field, environmental, or endurance
  validation.
- Unreal, packaging, signing, staging, release, deployment, rollback execution, or production
  validation.
- Target Quality CI or Portfolio Control validation on a commit containing P09.
