# P10 retained evidence — Move a Fault Threshold

**Batch:** P10
**Date:** 2026-09-01 UTC
**Guiding question:** What inputs, observable effects, and failure modes matter when you move a Fault Threshold?
**Disposition / evidence status:** implemented; P10 is `implemented` with evidence level `simulated`
after the isolated candidate and real worktree passed focused, contract, quick, full, and CLI
lifecycle gates, and the bounded system-risk and prerequisite-compatibility repairs passed the same
repository-local verification contract.

## Claim boundary

This record supports static repository structure, independent Python reference arithmetic,
synthetic deterministic residual/decision contracts, isolated CLI behavior, and retained command
output only. The intended manifest evidence level `simulated` classifies the module's synthetic
model; it does not mean MATLAB executed here. No MATLAB execution, rendered UI behavior, MATLAB
numerical fidelity, protocol, bench, HIL, RT1/RT2, field, playtest, staging, Unreal, signing,
deployment, production, or physical validation is claimed.

## Acceptance map

| Acceptance item | Implementation evidence | Current disposition |
| --- | --- | --- |
| Preserve the exact guiding question | Manifest identity plus README, lesson, walkthrough, `lesson.m`, and `experiment.m` | Static source present |
| Read → baseline → one lever → changed view → mechanism-first explanation | `lesson.m`, sectioned `experiment.m`, tutor lesson, and walkthrough | Static source present; human session unperformed |
| Deterministic baseline, labels, metrics, two sweeps, one broken case | Fixed 301-sample residual; residual/decision views; threshold and conditional-loss sweeps; wrong-sign boundary case | Independent references and static tests pass |
| Separate deterministic model and interaction | `model.m` owns calculations only; `interactive.m` owns three bounded controls, reset, four selectable views, and one `uiaxes` | Static source tests pass; MATLAB/UI unperformed |
| Connect to prerequisites without syntax-first teaching | P09 `r=y-y_hat(u)` sign and `L/min` unit precede P10's `r<=-T` rule, controls, and MATLAB mechanics | Static narrative review complete |
| Independent invariants, limits, interpretation, teach-back | `run_checks.m` and `checks.md` cover equations, window counts, confusion closure, pointwise comparisons, monotonic alarm sets, exact sweeps, the exposed ripple-control behavior, zero/full limits, malformed inputs, recovery, and teach-back; the focused Python suite also binds P10's matched-residual behavior to P09's source contract | Static inspection and Python references pass; MATLAB checks unperformed |
| Status changes only after focused and repository-wide verification | An isolated implemented/simulated copy passed 14 focused and 128 repository tests plus all three profiles before the real P10-only lifecycle transition; the post-audit state passed 15 focused and 129 repository tests; the first system-risk repair passed 16 focused and 130 repository tests; the prerequisite-compatibility repair passed 17 focused and 131 repository tests | Gate passed; ordering and final regression state retained |
| Honest evidence taxonomy | This claim boundary, runtime inventory, behavior matrix, residual risks, and unperformed-validation list | Present |

## Independent deterministic references

The retained Python tests independently calculate rather than execute MATLAB source:

- fixed time grid `0:0.1:30`, 301 samples;
- normalized command `0.5` before 10 seconds and `0.8` afterward;
- conditional loss activation at 20 seconds and P09 sign convention `r=y-y_hat(u)`;
- transparent P10 rule `r<=-T` for nonnegative `T`, inclusive at equality;
- equal high-command healthy and fault windows from 12–18 and 22–28 seconds, 60 samples each;
- baseline healthy residual range `[-0.1,+0.1] L/min`, mean `0 L/min`, fault range
  `[-1.7,-1.5] L/min`, and mean `-1.6 L/min`;
- baseline TN/FP/detected/missed counts `60/0/60/0`;
- threshold sweep `[0.06,0.12,0.50,1.49,1.56,1.72] L/min`, false-alarm fractions
  `[0.25,0,0,0,0,0]`, and detection fractions `[1,1,1,1,0.65,0]`;
- conditional-loss sweep `[0,0.04,0.06,0.08,0.20]`, post-fault means
  `[0,-0.32,-0.48,-0.64,-1.6] L/min`, and detection fractions `[0,0,0.45,1,1]`;
- interactive ripple-control regression at fixed `T=0.55 L/min` and `L=0.20`, with amplitudes
  `[0,0.40,0.60,0.70,1.00] L/min`, healthy false-alarm counts `[0,0,9,15,21]` of 60, and fault
  detection counts `[60,60,60,60,60]`;
- wrong-sign `r<=+T` baseline healthy false-alarm fraction `1` versus correct fraction `0`;
- pointwise residual isolation and monotonic alarm-set nesting across both experiment sweeps and the
  retained ripple-control regression.
- source-extracted P09/P10 sample period, command step, fault time, command levels, nominal gain,
  ripple frequency, sign/unit, and reference-window contracts are equal, and P09's default predictor
  gain remains matched to the shared nominal gain;
- independently reconstructed P09 matched-predictor and P10 residual records agree to 14 decimal
  places at zero, 6%, 20%, and full conditional loss with zero, baseline, or maximum ripple.

These references check intended arithmetic independently but are not MATLAB numerical-fidelity
evidence.

## Figure, control, metric, and unit inventory

### Experiment figures

1. P09 residual and signed `-T` boundary against time, in `L/min` and seconds.
2. Clear/alarm threshold decision against time, dimensionless state and seconds.
3. Healthy false-alarm and fault detection sample fractions against threshold magnitude, in
   fraction and `L/min`.
4. Fault detection and healthy false-alarm sample fractions against conditional effectiveness loss,
   in fraction and percent.
5. Residual with correct `-T` and broken `+T` boundaries, in `L/min` and seconds.

### Interactive controls

| Control | Unit | Static bounds/default |
| --- | --- | --- |
| Threshold magnitude `T` | L/min | `[0,3]`; `0.50` |
| Conditional effectiveness loss after 20 s | percent | `[0,100]`; `20` |
| Deterministic ripple amplitude | L/min | `[0,1]`; `0.10` |
| Visible view | categorical residual/decision/threshold tradeoff/loss tradeoff | residual and signed threshold default; one axes |
| Reset baseline | action | restores all three numeric inputs and the residual view |

### Model outputs and metrics

- Fixed 301x1 time, command, activation, ripple, fault-residual, residual, threshold, statistic,
  margin, alarm, reference-mask, and broken-comparator vectors.
- Scalar sample period/count, command/fault times, nominal gain, threshold magnitude, conditional
  loss, ripple frequency/amplitude, and explicit equation/unit/meaning contracts.
- Equal three-cycle healthy and fault reference masks; residual means and extrema.
- TN, FP, detected, and missed counts; false-alarm, detection, and missed sample fractions; expected
  post-fault mean; broken false-alarm count/fraction.
- Resource contract: three bounded scalars, fixed 301 samples, no input-sized loop, file, network,
  process, timer, asynchronous work, blocking prompt, or persistent/global state.

## Runtime inventory

| Item | Required by implementation | Observed in this environment |
| --- | --- | --- |
| Python 3 | Static verifier, independent arithmetic, isolated CLI tests | Python 3.11.2; executed |
| Base MATLAB | Model, figures, controls, and executable checks | Not installed; MATLAB execution unperformed |
| MATLAB toolboxes | None | Not queried because MATLAB is absent |
| Octave fallback | Not an approved substitute for the `uifigure` lesson | Not installed |
| Data/network/service | None | No module dependency; CI inspection was read-only |
| Hardware/bench/HIL/field | None for implementation | No validation performed |

## Focused behavior matrix

| Category | Retained check | Current result |
| --- | --- | --- |
| Positive | Exact residual/decision equations, sign/units, fixed windows, baseline counts, pointwise comparator, both experiment sweeps, exposed ripple-control behavior, P09/P10 matched-residual compatibility, monotonic alarm sets, and learner flow | Independent Python/static source pass |
| Negative | Wrong-sign `r<=+T` makes every healthy baseline sample alarm; high `T` and small loss create deterministic misses | Independent arithmetic/static source pass |
| Malformed input | MATLAB source rejects nonscalar, below/above-bound, NaN, Inf, and complex values for all three inputs | Present; MATLAB execution unperformed |
| Timeout | Shared CLI subprocess tests have ten-second limits; fixed model has no wait or variable work | Baseline CLI suite passed; model timing unmeasured |
| Cancellation | No request, job, timer, blocking prompt, partial write, or asynchronous lifecycle exists | Not applicable by static review |
| Rollback | Reversible file/status rollback below; no migration or database exists | Documented; not executed |
| Recovery | A valid baseline is recomputed exactly after rejected calls; CLI fixtures isolate learner state | Source present; MATLAB recovery unperformed |
| Isolation | Threshold sweep fixes residual/components; loss sweep fixes threshold/ripple and every pre-fault residual/alarm; ripple regression fixes command/fault/threshold while recomputing every alarm metric | Independent/static source pass |
| Compatibility | Transparent base-MATLAB arithmetic and established `uifigure` constructs; no toolbox path; source-extracted P09/P10 timing, command, gain, ripple, sign/unit, window, and matched-residual contracts are regression-tested | Static/independent Python pass; MATLAB equivalence and release matrix unperformed |
| Resource bound | Three bounded scalars, 301 samples, fixed vector sizes, no external or asynchronous operation | Static source pass |

## Repository, ownership, concurrency, runtime, and CI inspection

- Initial branch `agent/p10` was at exact `origin/main` commit
  `d145d81b715c13d3a889202c80e0b1bff0e35495`, containing implemented P09.
- Initial target status contained only the staged control-plane activation changes in forbidden
  `contracts/active-batch.yaml` and `contracts/repo-profile.yaml`; they were inspected and preserved.
- Before the final principal-agent audit began, an additional unstaged, provenance-only control
  refresh from `499512aee4e2ca1c2ca74c5f3bd5e7d66b5b1987` to
  `b2d464da48358794232ba229716bbfee12fa383d` was already present in those same forbidden files.
  The P10 implementation did not author, stage, revert, or otherwise modify either contract layer.
- The separate local-main worktree remains at baseline commit
  `e604208c90c5424e6bd9dbcc22837b0fb2228c32`.
- No target CODEOWNERS/OWNERS/MAINTAINERS, dedicated ADR, safety record, compatibility record, Git
  lock, open target pull request, or competing target writer was found in the point-in-time
  inspection. Canonical `repos.yaml` assigns module/curriculum/validation/evidence ownership to the
  target repository.
- The final `portfolio status reliability-fdir-learning` inspection still reported P10 next, no
  stop gate, target open PRs `0`, recorded control revision `b2d464d`, and pointer drift outside this
  batch. It also reported unrelated open control-plane pull requests.
- Target Quality run `33467630823` succeeded on exact starting HEAD `d145d81`; P10 CI cannot run
  without a forbidden commit/push.
- Control-plane validation run `33571963291` succeeded on exact currently recorded control revision
  `b2d464da48358794232ba229716bbfee12fa383d`.
- `matlab` and `octave` were absent from `PATH`; this is not a stop because the contract supplies a
  transparent base-MATLAB source and static independent-reference path.

## Independent audit findings and repairs

A bounded read-only pattern audit compared P08/P09 artifact, test, lifecycle, and evidence patterns
with the P10 scaffold. It recommended preserving P09's signed negative loss residual, sweeping
threshold and conditional loss independently, checking monotonic alarm sets, using the `r<=+T`
wrong-sign case, pinning `@model` in the UI, deriving axes from data, and keeping P10 tests permanent
rather than freezing P11 or the moving global frontier. Those recommendations shaped the candidate;
the auditor made no edits and produced no runtime evidence.

The first focused source pass exposed three content/test mismatches in addition to the expected
lifecycle, publication, and evidence gates: the README did not literally name the wrong-sign case,
the applicability prose split the phrase “not applicable” across a line, and prerequisite prose did
not literally name the known command. The sources were clarified without changing the governing
math. The repeated pre-lifecycle focused pass then left only the intentional manifest assertion;
the isolated implemented candidate passed every gate before the real lifecycle transition.

A bounded read-only math/MATLAB-source audit independently reproduced all baseline, sweep, and
limiting references; traced sign, units, inclusive comparison, UI handle lifetime, dynamic axes,
malformed-input rejection, recovery, and fixed resources; and found no material source defect. A
bounded read-only contract audit found that this evidence record still described the already
completed lifecycle as prospective and that its focused test accepted stale pending prose. The
record now contains exact executed results, and the permanent P10 evidence test requires final
`implemented`/`simulated` disposition while rejecting stale pending, scaffolded-manifest, and
prospective-review markers. The auditors made no edits and retained every runtime and physical gap.

## Independent system-risk review and repair

The independent system-risk review found one moderate missing-test risk, not a current model defect.
`interactive.m` exposes deterministic ripple amplitude as a bounded learner-facing control, but the
retained behavior covered only the threshold and conditional-loss sweeps. Baseline arithmetic, a
tiny nonzero component check, maximum-input finiteness, and source-token checks could all pass while
a future dead, cross-wired, or stale ripple path gave the learner incorrect nuisance alarm counts.

The bounded repair adds the independent Python behavior regression
`test_ripple_control_has_exact_isolated_reference_behavior` at five UI-reachable amplitudes. It
requires fixed command, fault activation, fault component, and threshold; exact pointwise
ripple-plus-fault residuals; monotonically growing healthy alarm sets; healthy false-alarm counts
`[0,0,9,15,21]`; and all 60 fault samples detected. Matching retained MATLAB assertions recompute
the alarm vector, TN/FP/detected/missed counts, and all sample fractions from independent equations
at every point. `checks.md` now names the behavior and isolation boundary.

No production model equation, callback, learner control, package/public entry point, lifecycle
value, or shared file changed. A fresh whole-diff review found no additional material P10-owned
correctness, concurrency, lifetime, resource, migration, compatibility, security, licensing,
recovery, or missing-test defect. The inherited control-plane changes under forbidden
`contracts/**` paths were inspected and preserved at that point. The MATLAB assertions remain
unexecuted because neither MATLAB nor an approved substitute is installed; this repair adds static
and independent reference evidence only.

## Principal implementation-agent reconciliation

A second bounded read-only math/MATLAB-source audit independently reproduced the baseline, both
experiment sweeps, the learner-facing ripple regression, the inclusive boundary, and the limiting
cases. It found no material P10 source defect. It also confirmed the P09 and P10 constants, timing,
sign, and units currently align. Exact cross-module MATLAB floating-point equivalence, future P09
drift, graphics, callback lifetime, and release compatibility remain unexecuted gaps rather than
implied evidence.

A second bounded contract/evidence audit found three documentation risks and no model defect:

- three Markdown metadata lines had trailing spaces, so the historical `git diff --check HEAD`
  claim did not describe the then-current file; the spaces were removed and the check was rerun;
- the later, pre-existing control-provenance refresh made the historical staged hashes incomplete
  as a description of current forbidden state; both staged and working-tree hashes are now retained
  below without touching either forbidden file;
- the original rollback sequence was safe only while P10 was the implementation frontier. The
  sequence below now requires reverse dependency order and selective shared-file edits so later
  batches cannot be stranded above a scaffolded prerequisite or erased accidentally.

Both auditors were read-only and produced no MATLAB, UI, bench, HIL, field, deployment, or
production evidence.

## Final independent system-risk review and repair

The final whole-diff system-risk review found one moderate compatibility missing-test risk, not a
current model defect. P10 states that it thresholds P09's matched residual, but each module
reconstructs the sample period, command/fault timing, command levels, nominal gain, ripple, sign,
units, and reference windows independently. Their own focused tests could remain green after a
one-sided prerequisite-interface drift because no P10 regression compared the two source contracts.
The resulting failure would be a plausible but incompatible P10 lesson whose threshold counts no
longer describe the P09 residual it claims to inherit.

The bounded repair adds
`test_p09_to_p10_matched_residual_behavior_contract_is_regressed` only under the allowed P10 test
path. It extracts and compares the seven shared scalar source contracts, checks both modules'
transparent time/command/fault/ripple and residual equations, P09's matched predictor gain, sign and
unit, and equal healthy/fault reference windows, then independently reconstructs both residual paths
at four boundary and representative loss/ripple cases. The records agree to 14 decimal places. This
converts silent source-level drift into a focused P10 failure without editing P09, either MATLAB
model, a public entry point, learner state, or any forbidden contract.

This is static/source and independent Python compatibility evidence only. Neither MATLAB model was
executed, so exact cross-module MATLAB floating-point equivalence, graphics, callbacks, and release
compatibility remain unverified.

## Exact validation commands and results

### Pre-change baseline

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 114 tests, `OK` |

### Initial source iteration while lifecycle remains scaffolded

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p10_module.py` | 0 | No output |
| Initial `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p10_module` | 1 | 14 tests ran; six expected lifecycle/publication/evidence or source-text failures; the three source-text findings were repaired |
| Repeated `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p10_module` | 1 | 11 tests passed; only intentional manifest, retained-evidence, and shared-publication gate assertions remained at that point |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_curriculum tests.test_learn_cli` | 1 | 10 tests ran; only intentional rich-artifact-versus-scaffold lifecycle assertion failed |
| `git diff --check HEAD` | 0 | No whitespace errors |
| Raw-byte EOF scan over P10 artifacts and focused test | 0 | Every checked text file ends with exactly one newline |

### Isolated implemented-candidate lifecycle gate

The isolated candidate contains the exact working source plus P10-only lifecycle values
`implemented`/`simulated`. Required candidate commands and results are recorded here after execution:

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p10_module` | 0 | 14 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 128 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 128 tests, `OK`; started `2026-09-01T04:11:27+00:00`; completed `2026-09-01T04:11:29+00:00`; log `/tmp/reliability-fdir-p10-candidate.psPD9R/repo/docs/evidence/local/verify-20260901-041127.log` |
| `./bin/learn start P10`, `./bin/learn check P10`, `./bin/learn status` | 0 | Exact title/question; `Status: implemented`; launch/check commands; 24 total and 10 implemented |

### Real-worktree verification after manifest transition

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p10_module` | 0 | 14 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 128 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | `VERIFY PASS: 24 modules`; 128 tests, `OK`; started `2026-09-01T04:11:45+00:00`; completed `2026-09-01T04:11:48+00:00`; log `docs/evidence/local/verify-20260901-041145.log` |
| Isolated-state `./bin/learn start P10`, `./bin/learn check P10`, `./bin/learn status` using the real manifest | 0 | Exact title/question; `Status: implemented`; check command; 24 total and 10 implemented |

The candidate and real-worktree full profiles executed the repository's exact
`contracts/verification.commands` sequence.

### Post-audit final verification

The independent limiting-case test and stale-evidence regression were present for every command in
this table.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `python -m py_compile tests/test_p10_module.py` | 0 | No output |
| `python -m unittest discover -s tests -p 'test_p10*.py' -v` | 0 | 15 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 129 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Exact contract sequence passed: `VERIFY PASS: 24 modules`; 129 tests, `OK`; started `2026-09-01T04:18:43+00:00`; completed `2026-09-01T04:18:45+00:00`; log `docs/evidence/local/verify-20260901-041843.log` |
| Isolated-state `./bin/learn start P10`, `./bin/learn check P10`, `./bin/learn status` | 0 | Exact title/question; `Status: implemented`; check command; 24 total, 10 implemented, and current P10 |
| `git diff --check HEAD` | 0 | No whitespace errors |
| Raw-byte EOF scan over every changed or untracked text file | 0 | Every checked text file ends with exactly one newline |
| `sha256sum` over the protected governance/build/CI set | 0 | Every value matches the initial hashes retained below |

### Post-system-risk repair verification

The ripple-control regression and matching MATLAB source assertions were present for every command
in this table.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m py_compile tests/test_p10_module.py` | 0 | No output |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_p10*.py' -v` | 0 | 16 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 130 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Exact contract sequence passed: `VERIFY PASS: 24 modules`; 130 tests, `OK`; started `2026-09-01T04:28:46+00:00`; completed `2026-09-01T04:28:48+00:00`; log `docs/evidence/local/verify-20260901-042846.log` |

### Principal-agent reconciliation verification

The forbidden control-provenance refresh, evidence-currentness repair, and frontier-aware rollback
repair were present for every command in this table. The contract files themselves remained
untouched by the P10 implementation.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_p10*.py' -v` | 0 | 16 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 130 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Exact contract sequence passed: `VERIFY PASS: 24 modules`; 130 tests, `OK`; started `2026-09-01T23:52:06+00:00`; completed `2026-09-01T23:52:08+00:00`; log `docs/evidence/local/verify-20260901-235206.log` |
| `git diff --check HEAD` | 0 | No whitespace errors after the retained-evidence repair |
| Raw-byte EOF scan over `git diff --name-only HEAD` | 0 | All 17 changed text files end with exactly one newline and no extra EOF blank line |

### Final independent system-risk repair verification

The prerequisite-compatibility regression and this updated evidence record were present for every
command in this table.

| Command | Exit | Relevant result |
| --- | ---: | --- |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p10_module.P10ModuleTests.test_p09_to_p10_matched_residual_behavior_contract_is_regressed` | 0 | 1 test, `OK` |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -p 'test_p10*.py' -v` | 0 | 17 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | 0 | 131 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | 0 | Exact contract sequence passed: `VERIFY PASS: 24 modules`; 131 tests, `OK`; started `2026-09-02T00:05:21+00:00`; completed `2026-09-02T00:05:23+00:00`; log `docs/evidence/local/verify-20260902-000521.log` |

## Changed and preserved invariants

- P10 owns one complete nine-file module artifact set, one permanent-fact focused test file, three
  shared learner entry-point/index updates, one P10-only manifest lifecycle transition after its
  gate, and this retained evidence.
- Exactly 24 modules retain canonical IDs, order, titles, questions, phases, folders, and
  earlier-only prerequisites; implemented modules remain a contiguous canonical prefix.
- P10 retains prerequisite P09 and the exact approved guiding question.
- The focused P10 suite now fails if P09 and P10 diverge in their matched-residual source contract;
  the P09 implementation remains read-only and unmodified by this batch.
- P01-P09 and P11-P24 implementation areas are not edited.
- P10-owned changes preserve every active-batch forbidden path, including inherited dirty
  contracts, CI, agent skills, phase data, architecture, implementation contract, tutor protocol,
  and Makefile. The staged activation blobs remain
  `4f8e981eecbb571f24dc5e82b9e68f56deedb3ca85a310662801a5a2b1064f9a` for the active batch and
  `95069d4f5462756e335269bc0257fb3a128575e6bd15297c73c987bf88cba333` for the repo profile. The
  already-present unstaged provenance refresh yields current working-tree hashes
  `9c689b55b27f4e55e86c58030c9b735656072dde2a6e9303ffd4dc13405f6f66` and
  `bc9b48c29bbff8b23439bffc92cd1fa42c6af0dc206b6cc2f7169a02858fa740`, respectively.
- Other protected SHA-256 values remain AGENTS
  `6e769a084d6583a4e35820803fa46f51749889c7bd30731443ddd62276390e84`, phases
  `a8cc51f8d28ec40bbf633605b5ffec778635873a43aacda410201cda2983bee3`, Makefile
  `1af33be25ab25c149afe328a319a7fbd9d67fae8b94a42943b6b797ead2a49e8`, and Quality workflow
  `0a2617414f5f8249a25ed23d1b08f1cb78ae68f2d02650cc6bc07590b11c8d71`.
- No toolbox, random input, data/network/service dependency, learner-progress file, migration,
  database, deployment operation, or shared mutable module state is introduced.
- No commit, push, pull request, merge, release, deployment, or setting change is performed.

## Rollback

Rollback is file-based and has not been executed. This direct P10 rollback is safe only while no
implemented downstream module depends on P10. If P11 or any later dependent batch has been
implemented, downstream batches must be rolled back first in reverse dependency order under their
own approved contracts. Shared files must be edited selectively; restoring an old whole-file copy
could erase legitimate later frontier work.

1. Restore the original six P10 scaffold files and remove P10 `model.m`, `interactive.m`, and
   `run_checks.m`.
2. Restore only P10 manifest status to `scaffolded` and evidence level to `none`.
3. Remove or revise only P10-owned publication text in `README.md`, `START_HERE.md`, and
   `modules/README.md`; preserve all unrelated and later-batch content.
4. Remove `tests/test_p10_module.py` and this retained evidence record.
5. Run all three active-batch validation commands and confirm P01-P09 remain the implemented prefix.

No backup restore, migration rollback, database recovery, service recovery, generated binary, or
external-system compensation is required.

## Residual risks

- MATLAB syntax, numerical behavior, graphics, callbacks, and `run_checks.m` remain unverified
  without a licensed MATLAB runtime.
- Static source checks cannot prove compatibility with every MATLAB release; no minimum release or
  release matrix is declared.
- P10 inherits P09's zero-delay, aligned, matched residual and omits model mismatch, sensor faults,
  command/measurement delay, calibration drift, saturation, correlated disturbances, and changing
  operating modes.
- A static one-sample threshold omits persistence, hysteresis, debounce, adaptation, and scheduling;
  these omissions are named rather than silently treated as solved.
- Fixed deterministic window fractions are not field false-alarm or detection probabilities, event
  rates, confidence bounds, latency evidence, or safety guarantees.
- Detection sensitivity does not establish fault specificity, posterior diagnosis, recovery safety,
  or mission-level impact.
- Static Python arithmetic is independent of MATLAB source but is not MATLAB numerical-fidelity or
  educational-effectiveness evidence.
- P10 reconstructs P09's matched residual algebraically instead of invoking P09 at runtime. Shared
  source contracts and independently reconstructed behavior are now regression-tested, but exact
  cross-module MATLAB floating-point equivalence and runtime coupling remain untested.
- Historical protected governance prose still names older frontiers/provenance; it is outside the
  P10 allowlist and preserved rather than silently rewritten.
- P10 target Quality and Portfolio Control CI cannot run without a commit or push, which the user
  explicitly forbids.

## Unperformed validation

- MATLAB execution of `model.m`, `experiment.m`, `interactive.m`, `lesson.m`, or `run_checks.m`.
- MATLAB numerical fidelity, profiler/timing/resource measurement, release compatibility matrix,
  toolbox/license resolution, or Octave execution.
- Rendered traces, axes, labels, legends, UI layout, callbacks, reset behavior, accessibility, or UI
  behavior and interaction cancellation.
- Human tutor walkthrough, interpretation answers, teach-back, learner completion, playtest, or
  learning-efficacy evaluation.
- Protocol validation, backup/restore smoke, migration/rollback rehearsal, and service smoke; no
  protocol, database, migration, backup, or service is introduced, so these profile gates are not
  applicable beyond documented review.
- Physical Pump-A/cooling-loop hardware, bench, HIL, RT1/RT2, field, environmental, or endurance
  validation.
- Unreal, packaging, signing, staging, release, deployment, rollback execution, or production
  validation.
- Target Quality CI or Portfolio Control validation on a commit containing P10.
