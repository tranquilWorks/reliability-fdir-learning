# P11 retained evidence — Isolate Competing Faults

**Batch:** P11
**Date:** 2026-09-02 UTC
**Guiding question:** What inputs, observable effects, and failure modes matter when you isolate Competing Faults?
**Disposition:** implemented; P11 is `implemented` with evidence level `simulated`.

## Claim boundary

This record supports static source inspection, independent deterministic Python arithmetic, synthetic-model contracts, isolated command-line behavior, and repository verification. The `simulated` evidence level describes the deterministic model class; it does not mean that MATLAB executed. MATLAB execution, rendered UI behavior, numerical fidelity, protocol, bench, HIL, RT1/RT2, field, playtest, Unreal, signing, deployment, and production validation were not performed.

## Acceptance map

| Acceptance item | Implemented artifact and evidence |
| --- | --- |
| Preserve P11 identity, question, order, phase, and P10 prerequisite | `curriculum/modules.json`; permanent identity test |
| Teach read, baseline, one changed lever/view, interpretation, and teach-back | `lesson.m`, `lesson.md`, and `walkthrough.md` |
| Show labeled baseline flow, pressure, and signature evidence | `experiment.m` baseline views; independent arithmetic tests |
| Include two isolated parameter sweeps | Pump-loss sweep with bias fixed at zero; sensor-bias sweep with loss fixed at zero |
| Expose a deliberately broken case | Flow-only decoder produces a two-exact-match ambiguity and names the violated distinct-signature assumption |
| Separate deterministic model and presentation | Pure `model.m`; one-axis, two-control `interactive.m` |
| Cover invariants, limits, malformed input, recovery, compatibility, and bounds | `run_checks.m`, `checks.md`, and `tests/test_p11_module.py` |
| Publish P11 without freezing a later frontier | Shared entry points and P11-owned permanent assertions |
| Retain honest evidence and rollback | This record, including explicit unperformed validation |

## Independent deterministic references

The focused Python suite independently reconstructs the fixed 301-sample P10-compatible record rather than parsing MATLAB outputs. It checks 60-sample healthy and fault windows, the Pump-A loss baseline means of `-1.6 L/min` and `-8 kPa`, sensor-bias means of `-1.6 L/min` and `0 kPa`, signatures `11` and `10`, exact Hamming distances, both five-point sweeps, exact inclusive feature boundaries, just-clear flow and pressure cases `128 eps(1)` below those boundaries, pointwise P10/P11 operation-order compatibility, full-loss and maximum-bias limits, the weak-pump `10` coverage gap, and the flow-only two-match ambiguity. It also verifies the identity `r_P - 5 r_Q = 5 B f` and marks simultaneous pump loss plus sensor bias outside the single-fault library.

## Figure, control, metric, and unit inventory

- Six experiment figures cover flow residual `r_Q` in `L/min`, pressure residual `r_P` in `kPa`, two-bit signature, Pump-A loss sweep in percent, sensor-bias sweep in `L/min`, and broken flow-only Hamming distance in bits.
- Interactive controls are bounded Pump-A loss `[0,100]%` and negative flow-sensor bias magnitude `[0,5] L/min`, plus a one-view selector and baseline reset. The UI owns one `uiaxes`.
- Metrics include fixed-window flow, pressure, and consistency means; P10 pointwise alarms; signature bits; Hamming distances; exact-match count; decoded label; correctness; and single-fault-library applicability.

## Runtime inventory

Python 3.11.2 executed. MATLAB and Octave were absent. The module adds no toolbox, data file, network, service, process, database, migration, backup, asynchronous job, hardware, bench, HIL, field, or production dependency. Model work is synchronously bounded by two validated scalar inputs and a fixed 301-sample vector.

## Focused behavior matrix

| Category | Evidence and disposition |
| --- | --- |
| Positive | Pump loss `0.20` decodes uniquely as Pump-A effectiveness loss (`11`) |
| Negative | An `0.08` weak loss produces `10` and demonstrates a documented library coverage gap; flow-only evidence is ambiguous; inputs just outside the numerical comparison tolerance remain clear rather than widening the decision band |
| Malformed input | Twelve scalar, range, real, finite, NaN, Inf, and complex rejection cases are encoded; MATLAB execution remains unperformed |
| Timeout | The model has no wait or asynchronous path; the shared learner CLI retains its ten-second bounded check timeout |
| Cancellation | Not applicable: the module has no asynchronous task, partial write, or external transaction to cancel |
| Rollback | Selective file rollback is documented below; execution was not required or performed |
| Recovery | Exact baseline recomputation after boundary cases is asserted in `run_checks.m`; MATLAB execution remains unperformed |
| Isolation | Each parameter sweep fixes the non-target fault at zero; independent tests verify no cross-component contamination |
| Compatibility | P10/P11 time, command, flow residual, threshold, and alarm behavior are source- and behavior-regressed |
| Resource bound | Inputs are bounded and the deterministic record is fixed at 301 samples |

## Repository, ownership, concurrency, runtime, and CI inspection

The target began on branch `agent/p11` at `647b34bc`, equal to `origin/main`. The only pre-existing worktree modification was the control-managed P11 activation in forbidden `contracts/active-batch.yaml`; its starting SHA-256 was `f26ae93f724e07c1a9b952f119ce57bbe6608e1e4b53b0b723e9c6584fb80e0f`. No ownership file, active lock, competing target writer, or open target pull request was found. Starting-head Quality run `33574027927` and recorded-control-revision validation run `33571963291` succeeded. P11 target CI is unperformed because commit and push are forbidden.

Canonical product and P11 batch artifacts, repository contracts, build files, tests, CI, handoff/current-state documents, prerequisite P10, and every active-batch implementation area were inspected before editing. No dedicated ADR, safety, or compatibility record was present. No explicit stop condition applied. The separately managed primary worktree remained outside this batch.

## Independent audit findings

Read-only governance, canonical-contract, and neighboring-module audits found no stop condition or competing writer. They supported preserving the exact P10 flow interface, using independently informative pressure evidence, making the weak-fault coverage gap explicit, breaking diagnosability by removing pressure, and keeping P11 tests permanent without asserting that P12 stays unimplemented or P11 remains the latest frontier. Final technical review found two boundary defects: P11's initial measured-minus-predicted operation order could differ from P10 at a pointwise floating boundary, and sampled-sine roundoff could clear analytically inclusive mean boundaries. The integration owner repaired both with P10's direct component sum, explicit `16 eps` comparison tolerances, and regressions at bias `0.50`, loss `0.10`, and loss `0.0625`.

A subsequent independent system-risk review found one material missing-test failure mode: exact-boundary inclusion was covered, but no behavioral guard proved that a sub-threshold feature outside the numerical tolerance stayed clear. A widened roundoff band could therefore have changed flow or pressure signature bits without failing the focused suite. The review added independent Python cases and retained MATLAB assertions at `128 eps(1)` below both analytic input boundaries. The just-clear sensor case remains `00`; the just-clear pressure case remains `10`. No P11 model equation or threshold was changed. The review rechecked correctness, public interfaces, ownership, concurrency, lifetime, resource bounds, migration, compatibility, security, licensing, recovery, rollback, and claim language and found no other material batch-owned defect. No MATLAB/runtime or physical evidence was produced.

## Exact validation commands and results

| Command | Environment | Exit | Result |
| --- | --- | ---: | --- |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py` | Real worktree, before edits | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v` | Real worktree, before edits | 0 | 131 tests, `OK` |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest tests.test_p11_module` | Isolated publication candidate | 0 | 16 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | Isolated publication candidate | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests` | Isolated publication candidate | 0 | 147 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | Isolated publication candidate | 0 | 147 tests, `OK`; log `docs/evidence/local/verify-20260902-003529.log` in the disposable candidate |
| `./bin/learn start P11`, `./bin/learn check P11`, and `./bin/learn status` | Isolated learner state | 0 | Exact P11 title/question, discoverable check command, and 11 implemented modules |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p11_module` | Real worktree, boundary-repaired P11 | 0 | 16 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py` | Real worktree, boundary-repaired P11 | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v` | Real worktree, boundary-repaired P11 | 0 | 147 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full ./scripts/agent-verify.sh` | Real worktree, boundary-repaired P11 | 0 | 147 tests, `OK`; retained local log `docs/evidence/local/verify-20260902-004648.log` |
| `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest -v tests.test_p11_module` | Real worktree, independent-review repair | 0 | 17 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=contract PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify.py` | Real worktree, independent-review repair | 0 | `VERIFY PASS: 24 modules` |
| `MATLAB_LEARNING_VERIFY_PROFILE=quick PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v` | Real worktree, independent-review repair | 0 | 148 tests, `OK` |
| `MATLAB_LEARNING_VERIFY_PROFILE=full PYTHONDONTWRITEBYTECODE=1 ./scripts/agent-verify.sh` | Real worktree, independent-review repair | 0 | 148 tests, `OK`; retained local log `docs/evidence/local/verify-20260902-005837.log` |

## Changed and preserved invariants

P11 now has a complete nine-file module artifact set, a permanent focused regression suite, retained evidence, and selective shared publication updates. Canonical ID, number, title, guiding question, phase, slug, folder, P10 prerequisite, implementation-batch ownership, contiguous implementation frontier, exact P10 zero-bias flow operation order and alarm behavior, inclusive analytical feature boundaries, just-clear behavior outside the numerical tolerance, later-module state, learner-local state, and every forbidden-path invariant are preserved. The tests assert permanent P11 facts and prerequisite compatibility; they do not require later batches to remain pending or call P11 permanently latest.

## Rollback

If later dependent batches exist, downstream batches must be rolled back first in reverse order. Shared files must be edited selectively: restore only P11 lifecycle fields and P11 publication text. Restore the six original P11 scaffold files, remove the three added MATLAB files, focused P11 test, and retained P11 evidence, then rerun all gates. There is no migration, external side effect, or compensation action. Rollback was not executed.

## Residual risks

MATLAB syntax, runtime numerics, graphics, callbacks, release compatibility, and profiler timing remain unverified. The pressure channel is an educational synthetic discriminator, not calibrated hardware evidence. Delay, drift, saturation, correlated disturbances, mode changes, multiple or unmodeled faults, online latency, persistence, field coverage, and educational effectiveness remain outside this batch's validated boundary.

## Unperformed validation

- MATLAB execution, rendered UI behavior, numerical fidelity, release-matrix execution, and profiler timing.
- Tutor session, learner prediction and teach-back, completion-state transition, accessibility review, and playtest.
- Protocol, backup/restore, migration, rollback rehearsal, and service smoke; these are not applicable to this file-only module beyond documented review.
- Bench, physical hardware, HIL, RT1/RT2, field, endurance, and environmental validation.
- Unreal, packaging, signing, staging, release, deployment, rollback execution, and production validation.
- P11 target Quality and Portfolio Control CI on a commit containing P11.
